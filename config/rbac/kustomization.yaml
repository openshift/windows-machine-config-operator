resources:
- service_account.yaml
# All RBAC will be applied under this service account in
# the deployment namespace. You may comment out this resource
# if your manager will use a service account that exists at
# runtime. Be sure to update RoleBinding and ClusterRoleBinding
# subjects if changing service account names.
- role.yaml
- role_binding.yaml
# NOTE: Removed wmco_cluster_rbac_role.yaml and wmco_serviceaccount_role.yaml
# These provided broad ClusterRole/ServiceAccount creation permissions for dynamic RBAC
# Now using static bundle manifests for better security
- wicd-certificate-group-clusterrole.yaml
- wicd-certificate-group-clusterrolebinding.yaml
- leader_election_role.yaml
- leader_election_role_binding.yaml
# Comment the following 4 lines if you want to disable
# the auth proxy (https://github.com/brancz/kube-rbac-proxy)
# which protects your /metrics endpoint.
#- auth_proxy_service.yaml
#- auth_proxy_role.yaml
#- auth_proxy_role_binding.yaml
#- auth_proxy_client_clusterrole.yaml
